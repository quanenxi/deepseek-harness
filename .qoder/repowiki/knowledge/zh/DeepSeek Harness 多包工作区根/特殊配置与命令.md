根脚本统一编排全仓任务：`pnpm build` 编译 host/client 库并构建 web；`pnpm test[:coverage|:e2e|:snapshot|:web]` 切换不同 vitest 配置；`pnpm check:all` / `check:ci:*` 通过 scripts/run-gates.ts 触发 CI 矩阵；`pnpm docs:dev|build|preview` 驱动 website；`pnpm dsh` 直接启动 CLI；`pnpm release:*` 完成版本 bump、打包与发布。