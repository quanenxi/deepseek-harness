`python/sdk/pyproject.toml` 通过 `[tool.uv.sources]` 将 `deepseek-harness-runtime-bin` 指向 `../sdk-runtime` 并以 `editable = true` 安装，因此运行 SDK 前需先执行仓库根脚本 `scripts/build-exe-for-python-sdk.ts` 生成 `runtime/dsh-jsonrpc-agent-pkg-*` 可执行文件；也可设置 `DSH_RUNTIME_MODE=node` 走开发期 node 闭包路径（要求系统 PATH 上有 Node ≥22.19）。