每个示例独立 `dsh` 运行；web-cordis/web-schedule 通过 `dsh web --patch examples/<dir>/cordis.yml` 以 patch overlay 方式叠加到 Web bundle；快照测试通过设置 `DSH_SNAPSHOT=record` 录制 JSONL 回放，并由 harness 在 `tests/snapshots` 下比对 session.jsonl/stdout.expected.jsonl。