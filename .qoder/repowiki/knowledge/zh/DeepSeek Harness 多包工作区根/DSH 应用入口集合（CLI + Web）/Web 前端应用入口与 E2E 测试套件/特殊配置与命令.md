不可直接执行 `vite dev` 或 `vite preview`，会被 `rejectStandaloneServe` 插件拒绝；必须通过仓库顶层 `pnpm dsh web` 启动（安装后使用 `dsh web`）。E2E 通过环境变量 `$DSH_SNAPSHOT` 切换模式：`replay`（默认，回放 session.jsonl）、`record`（真实 LLM 调用并抓取 fixture）、`refresh`（回放并重写黄金文件）。