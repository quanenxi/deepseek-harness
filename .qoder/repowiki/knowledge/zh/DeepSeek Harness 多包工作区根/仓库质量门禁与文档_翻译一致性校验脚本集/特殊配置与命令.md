通过 `pnpm run <gate>` 或 `node scripts/run-gates.ts <mode>` 触发，模式包括 `ci-primary`、`ci-static`、`ci-coverage`、`ci-snapshot`、`check-all`、`doc-sync`、`node-compat` 等；并发受 `DSH_GATE_CONCURRENCY` 控制，覆盖率工作线程数受 `DSH_COVERAGE_MAX_WORKERS` 控制，文档类型检查可通过 `DSH_DOC_TYPECHECK_USE_BUILD_OUTPUT=1` 复用构建产物。