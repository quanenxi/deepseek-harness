每个子包独立发布为 `@deepseek-ai/dsh-*` 包，构建产物位于 `lib/`，类型声明位于 `lib/types/`，并通过 `package.json` 的 `exports` 字段暴露 `.`、`./invariant`、`./types`、`./typert`、`./remote`、`./src/*` 等入口；测试位于各自 `tests/` 目录，使用 `.spec.ts` 命名约定。