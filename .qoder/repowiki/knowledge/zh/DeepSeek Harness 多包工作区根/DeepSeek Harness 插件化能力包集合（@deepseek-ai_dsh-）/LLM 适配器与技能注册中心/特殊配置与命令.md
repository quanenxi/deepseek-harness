每个子包独立发布为 npm 包（见各自 `package.json` 的 `name`、`publishConfig.access=public`、`exports` 字段），通过 workspace 依赖 `@deepseek-ai/dsh-llm`、`@deepseek-ai/dsh-settings`、`@deepseek-ai/dsh-session` 等内部包；插件需导出 `name`、`inject` 数组和 `apply(ctx, config)` 函数以便 Cordis 装配。