每个子包独立 `package.json`，通过 `lib/` 编译产物和 `exports` 字段暴露入口；ACP/Claude Code/Codex/DashScope 后端需额外安装对应运行时（如 `dsh-subprocess`、`dsh-timeout`、`@agentclientprotocol/sdk`）；测试统一位于各包 `tests/` 下，包含 `.spec.ts` 单元测试与 `.e2e.ts` 端到端用例。