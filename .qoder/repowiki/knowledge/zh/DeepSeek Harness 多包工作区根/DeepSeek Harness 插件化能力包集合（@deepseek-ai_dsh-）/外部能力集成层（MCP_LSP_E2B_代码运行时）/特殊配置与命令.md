各包均通过 `tsconfig.json` 编译到 `lib/`，测试位于各自 `tests/` 目录（如 `mcp-client.e2e.ts`、`typescript-server.e2e.ts`、`bootstrap.spec.ts`），需在工作区根安装 workspace 依赖后单独运行；`code-runtime-worker-thread` 额外通过 `tsdown.config.ts` 生成 CommonJS 入口 `lib/worker.cjs` 供 pkg VFS Worker 加载。