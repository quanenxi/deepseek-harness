每个子包独立发布为 npm 包（如 `@deepseek-ai/dsh-goal`、`@deepseek-ai/dsh-plan-mode`），通过 `package.json` 的 `exports` 字段暴露 `.`、`./invariant`、`./types`、`./client`、`./typert`、`./remote` 等多入口；构建产物位于 `lib/`，源码位于 `src/`，测试位于 `tests/`。