TypeScript + ESM；构建使用 tsdown（`tsdown.config.ts`）产出 `lib/index.js` 与 `lib/types/*.d.ts`；运行时基于 `@deepseek-ai/cordis` 的 Service/Fiber/Context 容器，使用 `node:crypto/randomUUID`、`node:async_hooks.AsyncLocalStorage`、`@deepseek-ai/schemastery` 进行配置校验与异步上下文传递。