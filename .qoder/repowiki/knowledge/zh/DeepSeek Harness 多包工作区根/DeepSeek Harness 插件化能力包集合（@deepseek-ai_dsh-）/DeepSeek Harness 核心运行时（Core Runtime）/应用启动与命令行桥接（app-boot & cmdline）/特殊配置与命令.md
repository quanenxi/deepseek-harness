构建产物位于各子包的 `lib/` 目录（由 tsdown/tsconfig 生成），源码在 `src/`；测试位于 `tests/` 目录下（如 `app-boot.spec.ts`、`cmdline.spec.ts`）。HMR 相关功能（`watchUserPatches`）仅在安装了 `@deepseek-ai/cordis-plugin-hmr` 时可用，该 peerDependency 标记为 optional。