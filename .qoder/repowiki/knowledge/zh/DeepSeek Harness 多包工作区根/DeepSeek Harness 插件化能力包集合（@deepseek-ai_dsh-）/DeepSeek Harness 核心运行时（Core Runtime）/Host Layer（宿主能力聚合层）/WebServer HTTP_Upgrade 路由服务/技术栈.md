Node.js `node:http` 原生服务器；Cordis 服务框架 (`@deepseek-ai/cordis`) 生命周期；Schemastery (`@deepseek-ai/schemastery`) 运行时配置校验；DSH Invariants (`@deepseek-ai/dsh-invariants`) 不变量守护；TypeScript + ESM (`"type": "module"`)。