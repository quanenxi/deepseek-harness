TypeScript + ESM (`"type": "module"`)；test-support 使用 vitest 进行快照与单元测试；llm-mock-server 基于 Node 原生 `http`/`crypto`/`timers/promises` 实现 OpenAI 兼容 SSE 流；attachment 基于 `@deepseek-ai/cordis` Service/Context 框架扩展上下文；构建产物位于各包 `lib/` 并通过 `tsconfig.json` 编译。