每个子包均为独立 npm package，拥有自己的 `package.json`、`tsconfig.json`、`README.md`/`README.zh.md`/`README.i18n.yaml` 及 `tests/` 目录；typert/generator 在测试中使用 `.generated-model-*` 临时目录产出宿主代码快照；fs/tool-fs-search 依赖打包的 `@vscode/ripgrep` 二进制，需确保其可在执行环境中找到。