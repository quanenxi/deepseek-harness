Node.js 原生 `crypto.randomUUID` / `fs` 同步 API；YAML 文档编辑使用 `yaml` 库的 `Document` 以保留注释格式；文件原子写入与锁通过 `@deepseek-ai/dsh-atomic-write` 的 `writeFileAtomic` 与 `withFileLock`；文件监控使用 `chokidar`；配置校验使用 `@deepseek-ai/schemastery`。