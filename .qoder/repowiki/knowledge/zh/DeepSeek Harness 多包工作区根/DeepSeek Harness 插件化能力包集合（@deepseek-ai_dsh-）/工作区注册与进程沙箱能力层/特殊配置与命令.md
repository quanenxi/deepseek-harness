`sandbox-local` 在首次 `confine` 时按平台链进行功能性探测（`probeBwrap`、`probeLandlock`、`probeSeatbelt`、`defaultProbeWindowsAcl`），探测超时由 `Config.probeTimeoutMs` 控制；Windows ACL 后端依赖已编译的 `lib/runner.js`，若不存在则回退到 `tsx/esm` 加载源码入口，测试可通过 `internals.platform`/`chain`/`probe*` 钩子替换任意平台路径。